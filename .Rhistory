from decimal import Decimal
from boto3.dynamodb.conditions import Key, Attr
from pandas import DataFrame
s <- dttm_to_timestamp(Sys.Date()-days(3))
from decimal import Decimal
from boto3.dynamodb.conditions import Key, Attr
from pandas import DataFrame
partition_key_name = "id"
sort_key_start = se
partition_key_name = "id"
partition_key_values = "4CFB"
sort_key_name = "timestamp"
sort_key_start = 1615680000000
sort_key_end = 1615939200000
value = "4CFB"
resp = dynamo_table.query(
# ConsistentRead= True,
KeyConditionExpression = Key(partition_key_name).eq(value) & Key(sort_key_name).between(Decimal(sort_key_start), Decimal(sort_key_end))
)
